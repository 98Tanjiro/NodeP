<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard</title>
    <!-- Liens vers les fichiers CSS de Bootstrap -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Styles CSS personnalisés -->
    <style>
        body {
            background-image: url('/pngtree-an-empty-conference-room-with-a-large-table-and-dark-curtains-picture-image_2457521.jpg');
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        
        .container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(255,255,255,0.5);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(48, 45, 45, 0.1);
        }
        
        .navbar {
            background-color: rgba(255,255,255,0.5);
        }
        
        .navbar-brand {
            color: #842204 !important;
            font-weight: bold;
        }
        
        .navbar-nav .nav-link {
            color: #842204 !important;
        }

        .btn-custom {
            background-color: #842204;
            border-color: #842204;
            border-radius: 20px;
            padding: 10px 20px;
            margin-bottom: 10px;
            display: block;
            text-align: center;
            color: #c2b8b8;
            text-decoration: none;
        }
        .text-center{
            color : #842204;
        }

        .btn-custom:hover {
            background-color: #ad2800;
            border-color: #702b06;
            text-decoration: none;
        }

        /* Footer */
        
    </style>
</head>
<body>
    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg">
        <a class="navbar-brand" href="#">Meeting room</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        <%= username %>
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="/users/logout">logout</a>
                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container mt-4">
        <h1 class="text-center">Welcome to our website</h1>
        <p class="text-center">Welcome <%= username %>, you are now connected as simple user</p>
        
        <div class="row">
            <div class="col-md-6">
                <a href="/salle/listesalle" class="btn btn-primary btn-custom">Consult available room list</a>
            </div>
            <div class="col-md-6">
                <a href="/reservations/reservations-en-cours" class="btn btn-primary btn-custom">Consult ongoing reservations</a>
            </div>
            
        </div>
    </div>

    <!-- Footer -->

    <!-- Liens vers les fichiers JavaScript de Bootstrap (optionnel) -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
