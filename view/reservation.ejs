<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Réserver la salle</title>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('/pngtree-an-empty-conference-room-with-a-large-table-and-dark-curtains-picture-image_2457521.jpg');
            background-color: #f8f9fa;
            font-family: Arial, sans-serif;
        }
        
        .container {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            background-color: rgba(255,255,255,0.5);
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(48, 45, 45, 0.1);
        }
        
        .navbar {
            background-color: rgba(255,255,255,0.5);
        }
        
        .navbar-brand {
            color: #842204 !important;
            font-weight: bold;
        }
        
        .navbar-nav .nav-link {
            color: #842204 !important;
        }

        .btn-custom {
            background-color: #842204;
            border-color: #842204;
            border-radius: 20px;
            padding: 10px 20px;
            margin-bottom: 10px;
            display: block;
            text-align: center;
            color: #c2b8b8;
            text-decoration: none;
        }
        .text-center{
            color : #842204;
        }

        .btn-custom:hover {
            background-color: #ad2800;
            border-color: #702b06;
            text-decoration: none;
        }

        
    </style>
</head>
<body>
    <!-- Barre de navigation -->
    <nav class="navbar navbar-expand-lg ">
        <a class="navbar-brand" href="#">Reservation for meeting room</a>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ml-auto">
                <li class="nav-item">
                    <a class="nav-link" href="/dashboard" style="color: #f8f9fa;">Back</a>
                </li>
            </ul> 
        </div>
    </nav>

    <!-- Contenu principal -->
    <div class="container mt-4">
        <h1>Reserve room</h1>
        <% if (salle) { %>
            <div class="alert alert-info" role="alert">
                <p>Information about room :</p>
                <ul>
                    <li><strong>Name of room :</strong> <%= salle.name %></li>
                    <li><strong>Capacity :</strong> <%= salle.capacity %></li>
                    <li><strong>location :</strong> <%= salle.location %></li>
                    <li><strong>Price/hour:</strong> <%= salle.prix %> Dinars</li>
                   
                </ul>
            </div>
        <% } %>
        <% if (successMessage) { %>
            <div class="alert alert-success" role="alert">
                <%= successMessage %>
            </div>
        <% } %>
        <% if (errorMessage) { %>
            <div class="alert alert-danger" role="alert">
                <%= errorMessage %>
            </div>
        <% } %>
        <form action="/reservations/reserve" method="POST">
            <div class="form-group">
                <label for="dateDebut">Start date :</label>
                <input type="date" class="form-control" id="dateDebut" name="dateDebut" required>
            </div>
            <div class="form-group">
                <label for="dateFin">End date:</label>
                <input type="date" class="form-control" id="dateFin" name="dateFin" required>
            </div>
            <input type="hidden" name="salleId" value="<%= salleId %>">
            <button type="submit" class="btn btn-primary">Reserve</button>
        </form>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.5.4/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
